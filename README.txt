Esta aplicação foi desenvolvida em Python e utiliza as seguintes bibliotecas:

- pymongo: para interagir com o MongoDB
- bcrypt: para hashing de senhas
- requests: para fazer requisições HTTP à API de consulta de CEP

Para instalar as dependências, execute:
pip install -r requirements.txt
